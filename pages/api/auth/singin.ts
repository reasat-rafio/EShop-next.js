import { NextApiRequest, NextApiResponse } from "next";

const signing = async (req: NextApiRequest, res: NextApiResponse) => {
    switch(req.method) {
        case "POST":
            
    }
};

export default signing;
